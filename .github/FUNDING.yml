# Puts a "Sponsor" button at the top of the repo.
#
# The button only appears once GitHub Sponsors is live on the account - until
# then GitHub ignores this and nothing is shown, which is the behaviour we
# want while the application is pending.
#
# SpikeSight is free regardless. Nothing in the app is ever gated behind this,
# and nothing ever will be.

github: [spikesight]
