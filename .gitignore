__pycache__/
*.pyc
*.spec
.venv/
dist/
build/
*.log

# Runtime state, if the app is ever pointed at the project folder. Anchored to
# the root on purpose: an unanchored "data/" also matches spikesight/data/,
# which is the bundled agent and map tables the app cannot start without.
/config.toml
/data/
